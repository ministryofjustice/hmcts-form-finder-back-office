- content_for :page_title do
  = "Add a user account to HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'User account management'}

.grid-row
  .column-two-thirds

    %h2.heading-medium
      Add a new user

    = render 'form'

  .column-one-third