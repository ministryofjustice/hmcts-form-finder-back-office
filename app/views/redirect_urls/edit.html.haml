- content_for :page_title do
  = "Edit a redirect in HMCTS Form finder"

.grid-row
  .column-full

    = render partial: 'layouts/header', locals: { page_heading: t('redirect_url.edit') }

= render 'form'
