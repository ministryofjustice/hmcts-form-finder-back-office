- content_for :page_title do
  = "Add a type to HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'Document types'}

.grid-row
  .column-two-thirds

    %h2.heading-medium
      Add a new type

    = render 'form'

  .column-one-third