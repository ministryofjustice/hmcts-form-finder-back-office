- content_for :page_title do
  = "Edit a document type in HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'Document type management'}

.grid-row
  .column-two-thirds

    %h2.heading-medium
      Edit a document type

    = render 'form'

  .column-one-third