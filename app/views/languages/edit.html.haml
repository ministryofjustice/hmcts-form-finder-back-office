- content_for :page_title do
  = "Edit a document language in HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'Document language management'}

.grid-row
  .column-two-thirds

    %h2.heading-medium
      Edit a language

    = render 'form'

  .column-one-third