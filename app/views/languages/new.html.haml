- content_for :page_title do
  = "Add a document language to HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'Document language management'}

.grid-row
  .column-two-thirds

    %h2.heading-medium
      Add a language

    = render 'form'

  .column-one-third
