- content_for :page_title do
  = "Edit a document in HMCTS Form finder"

#edit-document

  .grid-row
    .column-full
      = render partial: 'layouts/header', locals: { page_heading: t('document.edit') }

  = render 'updateform'
