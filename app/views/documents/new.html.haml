- content_for :page_title do
  = "Add a document to HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'Document management'}

.grid-row
  .column-two-thirds

    %h2.heading-medium
      Add a document

    = render 'form'

  .column-one-third