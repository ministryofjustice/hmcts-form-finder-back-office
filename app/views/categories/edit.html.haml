= render partial: 'layouts/header', locals: {page_heading: 'Categories management'}

%h2.heading-medium Edit a category

= render 'form'

// = link_to 'Show', @category
// = link_to 'Back', categories_path
