- content_for :page_title do
  = "Edit a document category in HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'Document category management'}

.grid-row
  .column-two-thirds

    %h2.heading-medium
      Edit a document category

    = render 'form'

  .column-one-third