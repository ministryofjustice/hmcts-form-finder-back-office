- content_for :page_title do
  = "Edit a category in HMCTS Form finder"

.grid-row
  .column-full

    = render partial: 'layouts/header', locals: {page_heading: 'Document categories'}

    %h2.heading-medium
      Edit a category

= render 'form'
