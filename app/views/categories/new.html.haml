- content_for :page_title do
  = "Add a category to HMCTS Form finder"

.grid-row
  .column-full

    = render partial: 'layouts/header', locals: {page_heading: 'Document categories'}

    %h2.heading-medium
      = t('headings.categories.add')

= render 'form'
