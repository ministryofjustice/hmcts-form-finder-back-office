- content_for :page_title do
  = "Document category management for HMCTS Form finder"

= render partial: 'layouts/header', locals: {page_heading: 'Document category management'}

.grid-row
  .column-full

    = link_to 'Create a new category', new_category_path, class: 'button'

    .table-container.container
      %table.report
        %caption
          %span.table-caption
            = @categories.size
            categories current available
        %colgroup
          %col
          %col
        %thead
          %tr
            %th{scope:'col'}
              = "English name"
            %th{scope:'col'}
              = "Welsh name"
            %th
              = "Inactive"
            %th
        %tbody
          - @categories.each do |category|
            %tr
              %td
                = category.english_name
              %td
                = category.welsh_name
              %td
                = category.inactive ? 'Yes' : 'No'
              %td
                = link_to 'Edit', edit_category_path(category), {class:"button-secondary", role:"button"}